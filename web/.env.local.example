# URL ของ FastAPI backend
# ทุก request ฝั่ง browser จะวิ่งผ่าน /api/* แล้วถูก rewrite ไปที่ค่านี้
NEXT_PUBLIC_API_URL=http://localhost:8000

# (optional) public site URL ใช้สำหรับ canonical/og:url
NEXT_PUBLIC_SITE_URL=http://localhost:3000
